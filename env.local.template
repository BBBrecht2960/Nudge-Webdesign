# Kopieer dit bestand naar .env.local en vul de waarden in.
# In de terminal: cp env.local.template .env.local
# .env.local wordt niet gecommit (staat in .gitignore).

# Supabase (verplicht voor database én admin-login)
# Haal de keys uit je Supabase-project: Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://juaawxiokaqqyispedxz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=vul_jouw_anon_public_key_in
SUPABASE_SERVICE_ROLE_KEY=vul_jouw_service_role_key_in

# PostHog – uncomment en vul in om analytics te verbinden (zie docs/POSTHOG.md)
# NEXT_PUBLIC_POSTHOG_KEY=jouw_project_api_key_van_posthog
# NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
# POSTHOG_API_KEY=jouw_personal_api_key_voor_server_side_queries (vind je in PostHog → Personal API Keys)
# NEXT_PUBLIC_POSTHOG_PROJECT_ID=jouw_project_id (optioneel, wordt automatisch gedetecteerd)

# Authentication (voor NextAuth, indien gebruikt)
# NEXTAUTH_SECRET=
# NEXTAUTH_URL=http://localhost:3000

# Bedrijfsgegevens
NEXT_PUBLIC_BUSINESS_PHONE=+32494299633
NEXT_PUBLIC_SITE_URL=https://yourwebsite.com

# Email (Resend)
# Maak een account op https://resend.com en haal je API key op
RESEND_API_KEY=re_U2cxCjpn_BSJbmy1zb3bUiVTxdPowJt2s
RESEND_DOMAIN=onboarding.resend.dev  # Optioneel: je eigen domain, anders gebruikt het onboarding.resend.dev
